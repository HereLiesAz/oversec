package io.oversec.one.crypto.sym

object SymUtil {
    fun byteArrayToHex(bytes: ByteArray): String {
        return ""
    }

    fun brcryptifyPassword(password: CharArray): ByteArray {
        return ByteArray(0)
    }

    fun calcKeyId(password: CharArray): Long {
        return 0L
    }

    fun calcSessionKeyId(keyId: Long, salt: ByteArray): ByteArray {
        return ByteArray(0)
    }
}
